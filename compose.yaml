services:
  production-server:
    build:
      context: .
      dockerfile: Dockerfile.production
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    environment:
      - COMPOSER_ALLOW_SUPERUSER=1
    restart: always
